# kaappari
Chrome Extension using CDP (Chrome DevTools Protocol) to command the Blink engine directly. Unlike "scroll-and-stitch" tools, it uses captureBeyondViewport for 100% accuracy. Features a custom guardrail logic to handle memory limits on huge pages, ensuring stability by dynamically optimizing image formats.
